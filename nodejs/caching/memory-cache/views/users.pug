extends layout
block content
  div.container
    form(action="/admin/users/search", method="post")
      input(type="text", name="name" placeholder="Search").search
    table
      thead
        tr.list-header
          th.list-header-th Name
          th.list-header-th Username
          th.list-header-th Email
          th.list-btn-container Proccess
      body
          tr.list
            td #{users[0].name}
            td #{users[0].username}
            td #{users[0].email}
            //- td #{moment(item.date).format('DD-MM-yyyy')}
            //- td #{new Date(item.date).toLocaleDateString('tr')}
            td.form-btn-container(style = "width:260px")
              a.form-btn.primary(href=`detailuser/${users[0].id}`) Details
              a.form-btn.warning(href=`edituser/${users[0].id}`) Edit
              a.form-btn.danger(href=`deleteuser/${users[0].id}`) Delete